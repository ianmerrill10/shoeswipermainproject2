# ShoeSwiper Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env files with actual secrets

# ===========================================
# CLIENT-SIDE VARIABLES (VITE_ prefix)
# These are exposed to the browser - safe for public keys only
# ===========================================

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Stripe Configuration (Public key only - safe for client)
VITE_STRIPE_PUBLIC_KEY=pk_test_your-stripe-public-key

# ===========================================
# AMAZON PA-API CONFIGURATION
# ===========================================

# Enable live Amazon prices (set to 'true' when PA-API is configured)
VITE_AMAZON_API_ENABLED=false

# Show prices in UI (set to 'true' when PA-API is returning live data)
VITE_SHOW_PRICES=false

# ===========================================
# SERVER-SIDE ONLY VARIABLES
# DO NOT prefix with VITE_ - these must never be exposed to the client
# ===========================================

# Supabase Service Key (full admin access - KEEP SECRET)
# SUPABASE_SERVICE_KEY=your-service-key

# Stripe Server-Side Keys
# STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
# STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# JWT Configuration
# JWT_SECRET=your-jwt-secret-at-least-32-characters

# Google Gemini API (MUST be server-side only for AI outfit analysis)
# GEMINI_API_KEY=your-gemini-api-key

# ===========================================
# AWS CONFIGURATION (Server-side only)
# ===========================================

# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# ===========================================
# AMAZON PA-API CREDENTIALS (Server-side only)
# These are set as Supabase Edge Function secrets:
#   supabase secrets set AMAZON_ACCESS_KEY=your-access-key
#   supabase secrets set AMAZON_SECRET_KEY=your-secret-key
#   supabase secrets set AMAZON_PARTNER_TAG=shoeswiper-20
# ===========================================

# ===========================================
# OPTIONAL: Feature Flags
# ===========================================

# VITE_DEMO_MODE=true
